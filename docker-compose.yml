#redis:
#  restart: always
#  image: redis:latest
#  ports:
#    - "6379:6379"

isbn_service1:
  build: src
  #links:
  #  - redis
  expose:
    - "8000"
  command: gunicorn rest.srv -w 2 -b :8000

web:
  build: web
  volumes:
    - /usr/share/isbn/
  command: tail -f /dev/null

nginx:
  build: nginx
  links:
    - isbn_service1:isbn_service1
    - web:web
  ports:
    - "80:80"
  volumes_from:
    - web
